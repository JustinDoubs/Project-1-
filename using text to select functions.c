#include <stdio.h>

int main(void)
{
    printf("please select an option:\n");
    printf("a) option 1");
    printf("b) option 2");
    
    
    while (c != 'a' && c != 'b'.......) 
    
    while (c < 'a' || c > 'z') //while c is less that 97 or while c is greater than ###
    
    switch(c) {
        case 'a' : option 1;
            break;
        case 'b' : option 2;
            break;
        default : do it properly you nong
        
        
        
        //look for char example talked about next week on blackboard 
        
        
    }
    
    
}
